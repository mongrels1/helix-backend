export const QUEUES = {
  SUBMISSION_CREATED: 'submission.created',
  ASSIGNMENT_OVERDUE: 'assignment.overdue',
  MASTERY_DROP_DETECTED: 'mastery.drop.detected',
  ATTENDANCE_RISK: 'attendance.risk.detected',
  ENGAGEMENT_DROP: 'engagement.drop.detected',
} as const;
