{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": { "startCommand": "uvicorn app:app --host :: --port $PORT", "healthcheckPath": "/health", "restartPolicyType": "ON_FAILURE" }
}
